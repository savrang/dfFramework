using UnityEngine;
using System.Collections;

public class dfPartsUnityObject : MonoBehaviour 
{
    public dfUnityObject[] _childParts = new dfUnityObject[dfDefine.MAX_PARTS_COUNT];

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
